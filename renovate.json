{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard"
  ],
  "labels": ["dependencies"],
  "prHourlyLimit": 0,
  "dependencyDashboardLabels": ["dependencies"],
  "dependencyDashboardAutoclose": true,
  "dependencyDashboardHeader": "Renovate updates for this repo. Every PR is CI-tested on 5 platforms (ubuntu x86/arm, debian, macos arm/intel) — merge when green. Checkboxes below force creation or rebase of the listed PRs.",
  "dependencyDashboardFooter": "Runs Mondays 08:00 UTC, or manually from the Actions tab. Config: [renovate.json](renovate.json) · [Renovate docs](https://docs.renovatebot.com/)",
  "customManagers": [
    {
      "customType": "regex",
      "description": "Pinned tool versions in install.sh (annotated with `# renovate:` comments)",
      "managerFilePatterns": ["/^install\\.sh$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+?)(?: versioning=(?<versioning>\\S+))?\\s*\\n\\s*[A-Za-z_]+=(?<currentValue>\\S+)"
      ],
      "versioningTemplate": "{{#if versioning}}{{{versioning}}}{{else}}semver{{/if}}"
    }
  ],
  "packageRules": [
    {
      "description": "Group all install.sh tool bumps into one PR",
      "matchManagers": ["custom.regex"],
      "groupName": "install.sh tools"
    },
    {
      "description": "Group all GitHub Actions bumps into one PR",
      "matchManagers": ["github-actions"],
      "groupName": "GitHub Actions"
    },
    {
      "description": "Exclude CI runner labels (macos/ubuntu) — CI infrastructure, bump deliberately",
      "matchManagers": ["github-actions"],
      "matchDepNames": ["macos", "ubuntu"],
      "enabled": false
    },
    {
      "description": "Exclude CI container images (debian) — CI infrastructure, bump deliberately",
      "matchDatasources": ["docker"],
      "enabled": false
    }
  ]
}
