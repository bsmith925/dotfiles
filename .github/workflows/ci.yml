name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  # ── Ubuntu x86_64 ────────────────────────────────────────────────────────
  ubuntu:
    name: ubuntu-latest (x86_64)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo
            ~/.rustup
          key: rust-${{ runner.arch }}-${{ hashFiles('install.sh') }}
          restore-keys: rust-${{ runner.arch }}-

      - uses: actions/cache@v4
        with:
          path: ~/.local/go
          key: go-${{ runner.arch }}-${{ hashFiles('install.sh') }}

      - name: install
        run: ./install.sh

      - name: test
        run: bash test.sh

  # ── Debian Bookworm x86_64 ───────────────────────────────────────────────
  debian:
    name: debian-bookworm (x86_64)
    runs-on: ubuntu-latest
    container:
      image: debian:bookworm
    steps:
      - uses: actions/checkout@v4

      - name: install
        run: ./install.sh

      - name: test
        run: bash test.sh

  # ── Ubuntu ARM64 ─────────────────────────────────────────────────────────
  ubuntu-arm:
    name: ubuntu-24.04 (arm64)
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v4

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo
            ~/.rustup
          key: rust-${{ runner.arch }}-${{ hashFiles('install.sh') }}
          restore-keys: rust-${{ runner.arch }}-

      - uses: actions/cache@v4
        with:
          path: ~/.local/go
          key: go-${{ runner.arch }}-${{ hashFiles('install.sh') }}

      - name: install
        run: ./install.sh

      - name: test
        run: bash test.sh
